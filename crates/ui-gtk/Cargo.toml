[package]
name = "dupdup-ui-gtk"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "dupdup-ui-gtk"
path = "src/main.rs"

[dependencies]
dupdup-core = { path = "../core" }

[features]
gtk = []

# Real GTK4 deps will be added once CI/toolchains are set up:
# [target.'cfg(target_os = "linux")'.dependencies]
# gtk4 = "0.9"

